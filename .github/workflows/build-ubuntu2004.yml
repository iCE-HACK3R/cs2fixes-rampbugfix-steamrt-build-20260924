name: Build CS2Fixes RampbugFix Ubuntu2004 fallback

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled during SteamRT ABI18 build; use build.yml.'
