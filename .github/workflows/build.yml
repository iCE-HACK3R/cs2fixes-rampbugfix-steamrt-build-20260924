name: Build CS2Fixes RampbugFix ABI18

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - 'artifacts/**'

concurrency:
  group: rampbugfix-abi18-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  linux:
    runs-on: ubuntu-latest
    container: registry.gitlab.steamos.cloud/steamrt/sniper/sdk:latest
    env:
      HL2SDKCS2: ${{ github.workspace }}/CS2Fixes-RampbugFix/sdk
    steps:
      - name: Checkout build repo
        uses: actions/checkout@v4
        with:
          path: buildrepo
          persist-credentials: true

      - name: Checkout RampbugFix
        uses: actions/checkout@v4
        with:
          repository: Interesting-exe/CS2Fixes-RampbugFix
          ref: main
          path: CS2Fixes-RampbugFix
          submodules: recursive
          fetch-depth: 0

      - name: Checkout Metamod 2.0.0.1469
        uses: actions/checkout@v4
        with:
          repository: alliedmodders/metamod-source
          ref: 2.0.0.1469
          path: mmsource-2.0
          submodules: recursive

      - name: Checkout AMBuild
        uses: actions/checkout@v4
        with:
          repository: alliedmodders/ambuild
          path: ambuild

      - name: Install AMBuild and clang
        shell: bash
        run: |
          apt-get update
          apt-get install -y clang-16 python3-setuptools
          ln -sf /usr/bin/clang-16 /usr/bin/clang
          ln -sf /usr/bin/clang++-16 /usr/bin/clang++
          cd ambuild
          python3 setup.py install --user
          cd ..
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Build package
        working-directory: CS2Fixes-RampbugFix
        shell: bash
        run: |
          set +e
          {
            echo '=== env ==='
            pwd
            ls -la
            echo "HL2SDKCS2=$HL2SDKCS2"
            echo '=== build ==='
            mkdir -p build
            cd build
            python3 ../configure.py --enable-optimize --sdks cs2 --targets=x86_64
            ambuild
            find . -maxdepth 6 -type f -name '*rampbugfix*' -print -exec file {} \; -exec sha256sum {} \;
            find package -maxdepth 8 -type f -print -exec sha256sum {} \;
            test -f package/addons/cs2fixes-rampbugfix/bin/linuxsteamrt64/cs2fixes-rampbugfix.so
            file package/addons/cs2fixes-rampbugfix/bin/linuxsteamrt64/cs2fixes-rampbugfix.so
            tar -C package -czf ../../CS2Fixes-RampbugFix-abi18-steamrt.tar.gz .
            sha256sum ../../CS2Fixes-RampbugFix-abi18-steamrt.tar.gz | tee ../../CS2Fixes-RampbugFix-abi18-steamrt.tar.gz.sha256
          } 2>&1 | tee ../rampbugfix-build.log
          exit ${PIPESTATUS[0]}

      - name: Commit artifact or failure log
        if: always()
        shell: bash
        run: |
          mkdir -p buildrepo/artifacts
          if [ -f CS2Fixes-RampbugFix-abi18-steamrt.tar.gz ]; then cp CS2Fixes-RampbugFix-abi18-steamrt.tar.gz buildrepo/artifacts/; fi
          if [ -f CS2Fixes-RampbugFix-abi18-steamrt.tar.gz.sha256 ]; then cp CS2Fixes-RampbugFix-abi18-steamrt.tar.gz.sha256 buildrepo/artifacts/; fi
          if [ -f CS2Fixes-RampbugFix/rampbugfix-build.log ]; then cp CS2Fixes-RampbugFix/rampbugfix-build.log buildrepo/artifacts/; fi
          cd buildrepo
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add artifacts || true
          if git diff --cached --quiet; then
            echo 'No artifact/log changes to commit.'
          else
            git commit -m 'Publish RampbugFix ABI18 build output'
            git pull --rebase origin main
            git push
          fi
